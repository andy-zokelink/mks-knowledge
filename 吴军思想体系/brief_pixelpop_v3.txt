# Pixel Pop v3 — 脚本注入数据

## 你的任务
1. `pixel_template.html` — Canvas 2D 像素拼贴，数据用 `{{ENTITIES_JSON}}` 占位
2. Python 注入脚本 → `pixel_pop.html`

## 视觉效果
- Canvas 2D，10,367 色块密矩阵（≈100×104）
- 按类别排序形成色温区块
- 三级 LOD：远纯点 → 中缩写 → 近全名
- 色块间 1px 暗线
- 滚轮缩放 + 拖拽平移
- 顶部缩放滑块 + 图例

## 验收
1. 注入 → 生成
2. node -c 语法
3. 占位符已替换
